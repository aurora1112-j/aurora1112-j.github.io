---
const { slug, title, titleEn, date, preview, previewEn } = Astro.props;
import RelativeDate from "../layouts/RelativeDate.astro";
import { Icon } from "astro-icon/components";
import LocalizedText from "../i18n/LocalizedText.astro";
---

<a href={`/${slug}`}>
	<div class="now-card">
		<Icon name="heroicons:clock" size={24} />
		<div>
			<h3><LocalizedText zh={title} en={titleEn || title} /></h3>
			{preview && <p class="description"><LocalizedText zh={preview} en={previewEn || preview} /></p>}
			<div class="metadata-container">
				<LocalizedText as="span" zh="此刻" en="Now Update" />
				<svg width="4px" height="4px">
					<circle r="2" cx="2" cy="2" fill="var(--color-gray-400)"></circle>
				</svg>
				<RelativeDate postDate={date} />
			</div>
		</div>
	</div>
</a>

<style>
	.metadata-container {
		display: flex;
		flex-direction: row;
		flex-wrap: wrap;
		align-items: center;
		margin-left: var(--space-xs);
		margin-top: 4px;
		font-family: var(--font-sans);
		font-size: var(--font-size-xs);
		color: var(--color-gray-500);
		text-transform: capitalize;
		font-weight: 400;
	}

	.now-card .metadata-container svg {
		margin: 0 0.4rem;
		position: relative;
		top: 1px;
	}

	.description {
		font-size: var(--font-size-xs);
		color: var(--color-gray-500);
		font-family: var(--font-sans);
		font-weight: 400;
		margin-left: var(--space-xs);
		margin-top: var(--space-2xs);
		padding-right: var(--space-s);
	}

	.now-card {
		display: flex;
		flex-direction: row;
		padding: var(--space-xs);
		margin: 0 var(--space-xs) var(--space-xs) 0;
		border-radius: var(--border-radius-base);
		border: 1px solid var(--color-cream);
		transition: all 0.3s ease-in-out;
		will-change: transform, background-color, box-shadow;
		cursor: pointer;
	}

	.now-card h3 {
		color: var(--color-gray-800);
		transition: color 0.3s ease-in-out;
		will-change: transform;
		font-family: var(--font-body);
		font-size: var(--font-size-base);
		font-weight: 400;
		line-height: var(--leading-snug);
		margin-left: var(--space-xs);
	}

	.now-card svg {
		position: relative;
		top: 4px;
		flex-shrink: 0;
		color: var(--color-sea-blue);
	}

	.now-card:hover {
		transform: scale3d(1.02, 1.02, 1.02);
		background: var(--color-light-cream);
		box-shadow: var(--box-shadow-sm);
		border: 1px solid var(--color-tinted-cream);
	}

	.now-card:hover h3 {
		color: var(--color-crimson);
	}
</style>
