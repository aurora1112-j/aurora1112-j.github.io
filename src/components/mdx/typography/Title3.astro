---
const { id } = Astro.props;
---

<h3 class="subsection-title" id={id}><slot /></h3>

<style>
	.subsection-title {
		font-size: var(--font-size-lg);
		font-family: var(--font-sans);
		color: var(--color-gray-800);
		line-height: var(--leading-tight);
	}
</style>
